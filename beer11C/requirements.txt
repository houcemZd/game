Django>=4.2,<5.2
channels>=4.0
channels-redis>=4.0
websockets>=12.0

# Production server (ASGI — handles HTTP + WebSocket)
daphne>=4.0

# Static-file serving without a separate web server
whitenoise>=6.6

# PostgreSQL driver (used on Render / Railway; SQLite is kept for local dev)
psycopg2-binary>=2.9

# Parse DATABASE_URL and REDIS_URL environment variables
dj-database-url>=2.1
